#include <stdio.h>

int main() {
    int t1, t2, t3;
    scanf("%d %d %d", &t1, &t2, &t3);

    double speed = (1.0 / t1) + (1.0 / t2) + (1.0 / t3);
    double time = 1.0 / speed;

    printf("%.2lf\n", time);

    return 0;
}
